@props(['label', 'value'])

<div class="rounded border border-slate-200 bg-white p-5">
    <p class="text-sm font-bold text-slate-500">{{ $label }}</p>
    <p class="mt-3 break-words text-2xl font-black text-courtigo-navy">{{ $value }}</p>
</div>
